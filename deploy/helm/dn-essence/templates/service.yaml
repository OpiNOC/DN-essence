apiVersion: v1
kind: Service
metadata:
  name: {{ include "dn-essence.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dn-essence.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dn-essence.selectorLabels" . | nindent 4 }}
